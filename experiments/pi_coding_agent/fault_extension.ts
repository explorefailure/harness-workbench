import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

throw new Error("intentional Harness Workbench extension load failure");

export default function registerFaultExtension(_pi: ExtensionAPI) {}
