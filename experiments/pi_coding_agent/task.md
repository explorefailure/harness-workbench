Use the available tool to carry out the requested fixture operation. This prompt is
intentionally ignored by the deterministic test provider; it exists so the complete
Pi invocation remains a normal non-interactive agent turn.
