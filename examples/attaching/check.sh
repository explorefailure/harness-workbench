#!/bin/sh
# Stands in for your workload. Stateless on purpose: the campaigns re-run this
# many times and compare, so anything it remembered would be what they measured.
echo "checking things"
exit 0
